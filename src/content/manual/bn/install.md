Naia OS প্রাথমিকভাবে লিনাক্স পরিবেশে ডিজাইন করা হয়েছে, Bazzite এর মতো ইমিউটেবল ওএস (Immutable OSs) অথবা Flatpak এর মতো সুরক্ষিত স্যান্ডবক্স পরিবেশের উপর গুরুত্ব দিয়ে।

## সিস্টেমের প্রয়োজনীয়তা

- **ওএস**: Linux (Bazzite, Ubuntu, Fedora, ইত্যাদি)
- **ডিসপ্লে সার্ভার**: Wayland (প্রস্তাবিত) অথবা X11
- **রানটাইম**: Node.js 22+ (যদি সোর্স থেকে বিল্ড করা হয়)
- **ডিমোন**: Systemd User Service সাপোর্ট প্রয়োজন

---

## স্থাপনার বিকল্প

যেহেতু Naia OS-এ এমন একটি এআই (AI) রয়েছে যা "সরাসরি ওএস নিয়ন্ত্রণ করে," তাই স্থাপনার পদ্ধতি একটি গুরুত্বপূর্ণ বিবেচনা।

### 1. Flatpak বিতরণ (প্রস্তাবিত)
এটি লিনাক্স ডেস্কটপ অ্যাপ বিতরণ করার সবচেয়ে সাধারণ এবং সুরক্ষিত উপায়। আপনি Discover অথবা GNOME Software এর মতো অ্যাপ স্টোরগুলির মাধ্যমে এটি সহজেই ইনস্টল করতে পারেন।

- **সুরক্ষিত আইসোলেশন**: অ্যাপের ইউআই (UI) (Tauri) এবং মূল এজেন্ট (Node.js) সম্পূর্ণরূপে স্যান্ডবক্সের ভিতরে চলে।
- **হোস্ট এক্সিকিউশন**: যখন এআই (AI) কে টার্মিনাল কমান্ড এক্সিকিউট (execute) করার প্রয়োজন হয় (যেমন, একটি প্যাকেজ ইনস্টল করা বা হোস্ট ফাইল সিস্টেম পরিচালনা করা), এটি নিরাপদে স্যান্ডবক্স থেকে বেরিয়ে আসে `flatpak-spawn --host` ব্যবহার করে।
- **ইনস্টল করার পদ্ধতি**:
  প্রদত্ত `.flatpak` বান্ডেলটি ডাউনলোড করুন এবং টার্মিনালের মাধ্যমে ইনস্টল করুন:
  ```bash
  flatpak install --user ./Naia-OS.flatpak
  ```

### 2. Bazzite / BlueBuild ইন্টিগ্রেটেড ইমেজ (উন্নত)
উন্নত ব্যবহারকারীদের জন্য, Naia OS একটি BlueBuild রেসিপি সরবরাহ করে অ্যাপটিকে সরাসরি একটি Bazzite ইমেজে বেক (bake) করার জন্য। এটি এআই (AI) কে ওএস-এর সাথে গভীরভাবে একত্রিত করে, যা বুট করার সাথে সাথে এআই (AI) অবতারকে আপনাকে শুভেচ্ছা জানাতে দেয়।

---

## 🛠️ (ডেভেলপারদের জন্য) লোকাল Flatpak বিল্ড গাইড

সোর্স কোড থেকে Flatpak স্যান্ডবক্স অ্যাপটি বিল্ড করতে, এই ধাপগুলি অনুসরণ করুন:

1. **Flatpak Builder ইনস্টল করুন**
   ```bash
   # Fedora / Bazzite
   sudo dnf install flatpak-builder
   
   # Ubuntu
   sudo apt install flatpak-builder
   ```

2. **বিল্ড ডিপেনডেন্সি যোগ করুন (SDK)**
   ```bash
   flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
   flatpak install --user flathub org.freedesktop.Platform//24.08 org.freedesktop.Sdk//24.08
   ```

3. **প্যাকেজিং বিল্ড চালান**
   এটি প্রোজেক্টের রুট ডিরেক্টরি-তে (Naia-OS) এক্সিকিউট করুন।
   ```bash
   # build-dir-এ কম্পাইল এবং বিল্ড করে
   flatpak-builder --user --install --force-clean build-dir flatpak/com.naia.shell.yml
   ```

4. **অ্যাপটি চালান**
   ```bash
   flatpak run com.naia.shell
   ```

> **💡 ডেভেলপার নোট:**
> ব্যবহারকারীর হোস্ট পরিবেশ ম্যানিপুলেট (manipulate) করার জন্য Flatpak স্যান্ডবক্সের ভেতর থেকে, এআই (AI) এজেন্ট অভ্যন্তরীণভাবে সমস্ত `execute_command` টুল রিকোয়েস্টকে `agent/src/gateway/tool-bridge.ts`-এ `flatpak-spawn --host bash -c ...` দিয়ে মোড়ানো (wrap) করে।