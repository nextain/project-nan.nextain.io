Naia OS مصمم بشكل أساسي لبيئات Linux، مع التركيز على أنظمة التشغيل غير القابلة للتغيير (Immutable OSs) مثل Bazzite أو بيئات الساندبوكس الآمنة مثل Flatpak.

## متطلبات النظام

- **نظام التشغيل**: Linux (Bazzite, Ubuntu, Fedora, إلخ.)
- **خادم العرض**: Wayland (موصى به) أو X11
- **وقت التشغيل**: Node.js 22+ (إذا كنت تقوم بالبناء من المصدر)
- **الخدمة الخلفية (Daemon)**: يتطلب دعم Systemd User Service

---

## خيارات النشر

نظرًا لأن Naia OS يتميز بذكاء اصطناعي "يتحكم في نظام التشغيل مباشرة"، فإن طريقة النشر تُعد اعتبارًا مهمًا.

### 1. توزيع Flatpak (موصى به)
هذه هي الطريقة الأكثر شيوعًا وأمانًا لتوزيع تطبيقات سطح مكتب Linux. يمكنك تثبيتها بسهولة عبر متاجر التطبيقات مثل Discover أو GNOME Software.

- **عزل آمن**: واجهة مستخدم التطبيق (Tauri) والوكيل الأساسي (Node.js) يعملان بالكامل داخل الساندبوكس.
- **تنفيذ المضيف**: عندما يحتاج الذكاء الاصطناعي إلى تنفيذ أوامر طرفية (مثل تثبيت حزمة أو إدارة نظام ملفات المضيف)، فإنه يخرج بأمان من الساندبوكس باستخدام `flatpak-spawn --host`.
- **كيفية التثبيت**:
  نزّل حزمة `.flatpak` المتوفرة وقم بتثبيتها عبر الطرفية:
  ```bash
  flatpak install --user ./Naia-OS.flatpak
  ```

### 2. صورة Bazzite / BlueBuild مدمجة (متقدم)
للمستخدمين المتقدمين، يوفر Naia OS وصفة BlueBuild لدمج التطبيق مباشرة في صورة Bazzite. يدمج هذا الذكاء الاصطناعي بعمق في نظام التشغيل، مما يسمح لصورة الذكاء الاصطناعي الرمزية بتحيتك فور الإقلاع.

---

## 🛠️ (للمطورين) دليل بناء Flatpak المحلي

لبناء تطبيق Flatpak sandbox من الكود المصدري، اتبع هذه الخطوات:

1. **تثبيت Flatpak Builder**
   ```bash
   # Fedora / Bazzite
   sudo dnf install flatpak-builder
   
   # Ubuntu
   sudo apt install flatpak-builder
   ```

2. **إضافة تبعيات البناء (SDK)**
   ```bash
   flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
   flatpak install --user flathub org.freedesktop.Platform//24.08 org.freedesktop.Sdk//24.08
   ```

3. **تشغيل بناء التعبئة**
   نفّذ هذا في دليل جذر المشروع (Naia-OS).
   ```bash
   # Compiles and builds into the build-dir
   flatpak-builder --user --install --force-clean build-dir flatpak/com.naia.shell.yml
   ```

4. **تشغيل التطبيق**
   ```bash
   flatpak run com.naia.shell
   ```

> **💡 ملاحظة للمطورين:**
> لمعالجة بيئة المضيف للمستخدم من داخل Flatpak sandbox، يقوم وكيل الذكاء الاصطناعي داخليًا بتغليف جميع طلبات أداة `execute_command` بـ `flatpak-spawn --host bash -c ...` في `agent/src/gateway/tool-bridge.ts`.