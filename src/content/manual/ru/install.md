Naia OS в первую очередь разработана для сред Linux, с акцентом на неизменяемые ОС, такие как Bazzite, или безопасные изолированные среды, такие как Flatpak.

## Системные требования

- **ОС**: Linux (Bazzite, Ubuntu, Fedora и т. д.)
- **Сервер отображения**: Wayland (рекомендуется) или X11
- **Среда выполнения**: Node.js 22+ (при сборке из исходного кода)
- **Демон**: Требуется поддержка пользовательского сервиса Systemd

---

## Варианты развертывания

Поскольку Naia OS включает ИИ, который "непосредственно управляет ОС", метод развертывания является важным аспектом.

### 1. Распространение Flatpak (рекомендуется)
Это наиболее распространенный и безопасный способ распространения настольных приложений Linux. Вы можете легко установить его через магазины приложений, такие как Discover или GNOME Software.

- **Безопасная изоляция**: Пользовательский интерфейс приложения (Tauri) и основной агент (Node.js) полностью работают внутри изолированной среды.
- **Выполнение на хосте**: Когда ИИ необходимо выполнить команды терминала (например, установить пакет или управлять файловой системой хоста), он безопасно выходит из изолированной среды с помощью `flatpak-spawn --host`.
- **Как установить**:
  Загрузите предоставленный пакет `.flatpak` и установите его через терминал:
  ```bash
  flatpak install --user ./Naia-OS.flatpak
  ```

### 2. Интегрированный образ Bazzite / BlueBuild (расширенный)
Для опытных пользователей Naia OS предлагает рецепт BlueBuild для непосредственной интеграции приложения в образ Bazzite. Это глубоко интегрирует ИИ в ОС, позволяя аватару ИИ приветствовать вас сразу после загрузки.

---

## 🛠️ (Для разработчиков) Руководство по локальной сборке Flatpak

Чтобы собрать приложение Flatpak в изолированной среде из исходного кода, выполните следующие шаги:

1. **Установите Flatpak Builder**
   ```bash
   # Fedora / Bazzite
   sudo dnf install flatpak-builder
   
   # Ubuntu
   sudo apt install flatpak-builder
   ```

2. **Добавьте зависимости сборки (SDK)**
   ```bash
   flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
   flatpak install --user flathub org.freedesktop.Platform//24.08 org.freedesktop.Sdk//24.08
   ```

3. **Запустите сборку пакета**
   Выполните это в корневом каталоге проекта (Naia-OS).
   ```bash
   # Compiles and builds into the build-dir
   flatpak-builder --user --install --force-clean build-dir flatpak/com.naia.shell.yml
   ```

4. **Запустите приложение**
   ```bash
   flatpak run com.naia.shell
   ```

> **💡 Примечание для разработчиков:**
> Чтобы управлять средой хоста пользователя изнутри изолированной среды Flatpak, агент ИИ внутренне оборачивает все запросы инструмента `execute_command` командой `flatpak-spawn --host bash -c ...` в файле `agent/src/gateway/tool-bridge.ts`.