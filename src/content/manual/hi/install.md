Naia OS को मुख्य रूप से लिनक्स वातावरण के लिए डिज़ाइन किया गया है, जो Bazzite जैसे अपरिवर्तनीय OSs या Flatpak जैसे सुरक्षित सैंडबॉक्स वातावरण पर केंद्रित है।

## सिस्टम आवश्यकताएँ

- **OS**: Linux (Bazzite, Ubuntu, Fedora, आदि)
- **डिस्प्ले सर्वर**: Wayland (अनुशंसित) या X11
- **रनटाइम**: Node.js 22+ (यदि स्रोत से निर्मित कर रहे हैं)
- **डेमन**: Systemd User Service समर्थन की आवश्यकता है

---

## परिनियोजन विकल्प

चूंकि Naia OS में एक AI है जो "OS को सीधे नियंत्रित करता है," परिनियोजन विधि एक महत्वपूर्ण विचार है।

### 1. Flatpak वितरण (अनुशंसित)
यह लिनक्स डेस्कटॉप ऐप्स वितरित करने का सबसे सामान्य और सुरक्षित तरीका है। आप इसे Discover या GNOME Software जैसे ऐप स्टोर के माध्यम से आसानी से इंस्टॉल कर सकते हैं।

- **सुरक्षित अलगाव**: ऐप का UI (Tauri) और कोर एजेंट (Node.js) पूरी तरह से सैंडबॉक्स के अंदर चलते हैं।
- **होस्ट निष्पादन**: जब AI को टर्मिनल कमांड निष्पादित करने की आवश्यकता होती है (उदाहरण के लिए, एक पैकेज स्थापित करना या होस्ट फ़ाइल सिस्टम का प्रबंधन करना), तो यह `flatpak-spawn --host` का उपयोग करके सुरक्षित रूप से सैंडबॉक्स से बाहर निकलता है।
- **कैसे स्थापित करें**:
  प्रदान किए गए `.flatpak` बंडल को डाउनलोड करें और इसे टर्मिनल के माध्यम से स्थापित करें:
  ```bash
  flatpak install --user ./Naia-OS.flatpak
  ```

### 2. Bazzite / BlueBuild एकीकृत इमेज (उन्नत)
उन्नत उपयोगकर्ताओं के लिए, Naia OS सीधे Bazzite इमेज में ऐप को एकीकृत करने के लिए एक BlueBuild रेसिपी प्रदान करता है। यह AI को OS में गहराई से एकीकृत करता है, जिससे AI अवतार आपको बूट होते ही तुरंत बधाई दे सकता है।

---

## 🛠️ (डेवलपर्स के लिए) लोकल Flatpak बिल्ड गाइड

स्रोत कोड से Flatpak सैंडबॉक्स ऐप बनाने के लिए, इन चरणों का पालन करें:

1. **Flatpak बिल्डर स्थापित करें**
   ```bash
   # Fedora / Bazzite
   sudo dnf install flatpak-builder
   
   # Ubuntu
   sudo apt install flatpak-builder
   ```

2. **बिल्ड डिपेंडेंसी (SDK) जोड़ें**
   ```bash
   flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
   flatpak install --user flathub org.freedesktop.Platform//24.08 org.freedesktop.Sdk//24.08
   ```

3. **पैकेजिंग बिल्ड चलाएँ**
   इसे प्रोजेक्ट रूट डायरेक्टरी (Naia-OS) में निष्पादित करें।
   ```bash
   # Compiles and builds into the build-dir
   flatpak-builder --user --install --force-clean build-dir flatpak/com.naia.shell.yml
   ```

4. **ऐप चलाएँ**
   ```bash
   flatpak run com.naia.shell
   ```

> 💡 **डेवलपर नोट:**
> Flatpak सैंडबॉक्स के अंदर से उपयोगकर्ता के होस्ट वातावरण में हेरफेर करने के लिए, AI एजेंट `agent/src/gateway/tool-bridge.ts` में सभी `execute_command` टूल अनुरोधों को आंतरिक रूप से `flatpak-spawn --host bash -c ...` के साथ लपेटता है।